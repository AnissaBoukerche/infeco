<section class="container my-3">
    <div class="row">
        <div class="col">
    {{ form_start(form) }}
    {{ form_row(form.address)}}
    {{ form_row(form.additionalAddressDetails) }}
    {{ form_row(form.city) }}
    {{ form_row(form.zipCode) }}
    <button class="btn btn-lg btn-primary mt-2" type="submit"> {{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
		</div>
	</div>
</section>


