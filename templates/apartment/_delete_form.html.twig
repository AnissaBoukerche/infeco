<form method="post" action="{{ path('app_apartment_delete', {'id': apartment.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ceci ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ apartment.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
